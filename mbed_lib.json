1 {
    "name": "mbed-jReadWrite",
    "config": {
        
        "jWrite_StackDepth": {
            "help": "max nesting depth of objects/arrays",
            "macro_name": "JWRITE_STACK_DEPTH",
            "value": 32,
            "required": true
        },
        "jWrite_OutputStringControl": {
            "help": "output string control for jwOpen()",
            "macro_name": "JW_COMPACT",
            "value": ,
            "required": true
        },
        "jWrite_PrettyIndentation": {
            "help": "pretty adds \n and indentation",
            "macro_name": "JW_PRETTY",
            "value": 1,
            "required": true
        },
        
    },
    "macros": [
        //"JW_GLOBAL_CONTROL_STRUCT", //comment this out to use applic-supplied jWriteControl
        //"JREAD_DOUBLE_QUOTE_IN_QUERY" //comment this out to use double quote insted of single, i.e.  "{\"key\"" instead of "{'key'"
        "EXPORT_OPTIONAL_FUNCTIONS" // Export Optional Helper Functions in jRead.h
    ],
    "target_overrides": {
        
    }
}